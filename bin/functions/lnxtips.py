import random
import os
os.system('clear')
facts = ["Use 'cf' to copy files.", "You can use 'rc' to compile python", "B-Kernel has a discord server!", "'ded' Deletes empty directories, while 'dd' Deletes directories and their contents.", "B-Kernel Web is the online version of B-Kernel!"]
print("[i] - " + facts[random.randint(0, 5)])