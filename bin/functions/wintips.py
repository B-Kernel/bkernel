import random
import os
os.system('cls')
facts = ["Use 'cf' to copy files.", "You can use 'rc' to compile python", "Pressing Shift + Click on the Taskbar [when clicking on a window] opens up another window.","B-Kernel has a discord server!", "'ded' Deletes empty directories, while 'dd' Deletes directories and their contents.", "B-Kernel Web is the online version of B-Kernel!"]
print("[i] - " + facts[random.randint(0, 5)])
